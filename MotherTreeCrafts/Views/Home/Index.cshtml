@model IEnumerable<MotherTreeCrafts.Models.Product>

@{
    ViewData["Title"] = "Home Page";
}

<!-- Minor changes needed to allow for a better "end" product -->

<div class="container-fluid">
    <div class="text-center mb-4">
        <h2 class="display-5 text-white">Welcome to Mother Tree Crafts</h2>
        <p class="lead text-white">Handcrafted treasures and custom 3D prints</p>
    </div>

    @if (Model != null && Model.Any())
    {
        <div class="products-grid">
            @foreach (var product in Model)
            {
                <a asp-controller="Products" asp-action="Details" asp-route-id="@product.ProductId" class="text-decoration-none">
                    <div class="product-tile">
                        @if (!string.IsNullOrEmpty(product.ImageUrl))
                        {
                            <img src="@product.ImageUrl" class="product-tile-img" alt="@product.Title">
                        }
                        else
                        {
                            <div class="product-tile-img d-flex align-items-center justify-content-center">
                                <i class="bi bi-image" style="font-size: 4rem; color: #666;"></i>
                            </div>
                        }
                        
                        <div class="product-tile-body">
                            <h5 class="product-tile-title">@product.Title</h5>
                            @if (!string.IsNullOrEmpty(product.Description))
                            {
                                <p class="text-muted small mb-2">
                                    @(product.Description.Length > 60 ? product.Description.Substring(0, 60) + "..." : product.Description)
                                </p>
                            }
                            <p class="product-tile-price">@product.Price.ToString("C")</p>
                            @if (product.IsFeatured)
                            {
                                <span class="badge bg-warning text-dark">Featured</span>
                            }
                        </div>
                    </div>
                </a>
            }
        </div>
    }
    else
    {
        <div class="text-center text-white">
            <h3>No products available at this time</h3>
            <p>Check back soon for new handcrafted items!</p>
        </div>
    }
</div>
